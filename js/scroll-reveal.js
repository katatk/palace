window.sr = ScrollReveal({
    reset: true
});

// Scroll reveal calls
window.sr = ScrollReveal();
sr.reveal('.pencil', {
    delay: 200,
    opacity: 1,
    duration: 800,
    origin: 'left',
    distance: '100%'
});
